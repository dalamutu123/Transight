# Transight

Enterprise transaction intelligence platform for payment operations teams — upload, validate, monitor, investigate, and report on transaction activity.

## Structure
- `frontend/` — React + Vite + TypeScript + Tailwind + MUI
- `backend/` — Express + TypeScript + Prisma + PostgreSQL

## Setup
See setup instructions in each folder's README (coming soon).